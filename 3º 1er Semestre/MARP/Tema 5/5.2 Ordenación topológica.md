- Consiste en ordenar los vértices de manera que todos los vértices partan de un vértice hacia todos, esto se utiliza de forma que tengamos que ordenar por ejemplo varias tareas que dependen de otras
- Se realiza con grafos dirigidos, a través de un recorrido Postorden del grafo, siendo la ordenación topológica el inverso del mismo
- No tiene porque ser única pero lo más importante es que no haya ciclos
- TODO GRAFO DIRIGIDO SIN CICLOS TIENE UNA ORDENACIÓN TOPOLÓGICA
- Recorrido Postorden:
	- Se empieza por el primer vértice y se va realizando un recorrido en profundidad del mismo hasta llegar a un vértice donde ya no se realicen más llamadas (no le quedan vértices no visitados por comprobar), se agrega al vector y se vuelve al siguiente vértice, así hasta recorrerlos todos
	- ![[Pasted image 20260607204411.png]]
	- La ordenación topológica de este grafo Y DEL RESTO es el inverso de este postorden: 3 6 0 5 2 1 4
- Propiedades:
	- Porque el inverso del postorden es una ordenación topológica
	- Demostramos que v->w
		- Caso 1: dfs(w) ya se hizo y terminó, entonces como primero se hizo v w depende de v
		- Caso 2: dfs(w) no se ha hecho, entonces v dependerá de w y por tanto estará bien
		- Caso 3: dfs(w) comenzó pero no ha terminado, ESTO ES IMPOSIBLE  ya que de ser así habría un ciclo y no se podría hacer la ordenación topológica
- Implementación:
	- Es hacer un dfs añadiendo una cola que haga push_front del vértice v
	- Complejidad V + A